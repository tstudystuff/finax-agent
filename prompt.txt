Create the complete frontend for the Finex Agent dividend page.

IMPORTANT OUTPUT RULES:

Return ONLY the raw source code for the requested file.

Do NOT use Markdown code fences.

Do NOT include explanations.

Do NOT include ```html.

Do NOT include ```css.

Do NOT include numbered steps.

Do NOT include terminal commands.

Do NOT include escaped HTML such as `\<html>`.

Return normal, valid source code exactly as it should appear inside the file.

I will redirect your output directly into a file.

PROJECT:

/Users/tt/ai-repos/finax-agent

Create the following two frontend files:

1. `index.html`
2. `css/style.css`

The project already contains the working Nasdaq Selenium scraper and its data. Do NOT modify the scraper.

The current data file is:

`data/next_divs.json`

The JSON has this structure:

{
"date": "2026-08-26",
"dividends": [
{
"ticker": "RAND",
"company": "Rand Capital Corporation Common Stock",
"declaration_date": "7/30/2026",
"ex_dividend_date": "8/26/2026",
"record_date": "8/26/2026",
"payment_date": "9/09/2026",
"amount": "0.29",
"ticker_url": "https://www.nasdaq.com/market-activity/stocks/rand/dividend-history"
},
{
"ticker": "SATA",
"company": "Strive, Inc. Variable Rate Series A Perpetual Preferred Stock",
"declaration_date": "7/14/2026",
"ex_dividend_date": "8/26/2026",
"record_date": "8/26/2026",
"payment_date": "8/27/2026",
"amount": "0.0516",
"ticker_url": "https://www.nasdaq.com/market-activity/stocks/sata/dividend-history"
}
]
}

## INDEX.HTML REQUIREMENTS

Create a complete valid HTML5 document.

The page title should be:

Finex Agent

The page should have a main heading:

Finex Agent

Create an Upcoming Dividends section.

The table must have exactly these visible columns:

Ticker
Company
Declaration Date
Ex-Dividend Date
Record Date
Payment Date
Amount

Do NOT display a Ticker URL column.

Do NOT hardcode RAND, SATA, or any dividend records into the HTML.

The HTML must load:

`css/style.css`

The HTML must also load a JavaScript file at:

`js/app.js`

The JavaScript file will be created separately later.

The table should contain an empty `<tbody>` that JavaScript can populate.

Display an element where JavaScript can place the dataset date from the JSON.

For example, JavaScript should eventually be able to change it to:

Upcoming Dividends — August 26, 2026

Do NOT hardcode August 26, 2026 into the HTML.

## TICKER REQUIREMENT

The first table column is extremely important.

The visible content of the ticker cell must be ONLY the ticker letters.

For example:

RAND

The ticker must eventually be rendered as an `<a>` element.

The anchor must use the `ticker_url` from the JSON data as its `href`.

The final generated HTML from JavaScript should be equivalent to:

<a href="https://www.nasdaq.com/market-activity/stocks/rand/dividend-history" target="_blank" rel="noopener noreferrer">RAND</a>

The user should see only:

RAND

When RAND is clicked, the Nasdaq dividend-history URL must open in a new browser tab.

Do NOT display the raw URL.

Do NOT hardcode the URL.

The JavaScript will obtain both `ticker` and `ticker_url` from the JSON data.

## RESPONSIVE DESIGN

Create clean, modern CSS appropriate for a personal financial dashboard.

The page should:

* look good on desktop
* work on mobile
* have a centered content area
* have a clear Finex Agent heading
* have a clean Upcoming Dividends section
* have a readable table
* have clear table headers
* have comfortable spacing
* make ticker links visually recognizable as clickable
* handle long company names
* allow the table to scroll horizontally on very small screens if necessary

Keep the design professional but simple.

Do not use external CSS frameworks.

Do not use Bootstrap.

Do not use Tailwind.

Do not use external dependencies.

Use ordinary CSS.

## IMPORTANT SCOPE LIMIT

Only create the HTML and CSS.

Do NOT create:

* JavaScript
* Python
* Selenium code
* JSON
* CSV
* databases
* APIs
* authentication
* portfolios
* tax features
* expense features
* charts

The existing data and scraper are already handled elsewhere.

## FINAL OUTPUT

Because the output will be redirected directly into files:

First return ONLY the complete contents of `index.html`.

After that, stop.

Do not return the CSS in the same response.

Do not explain anything.
